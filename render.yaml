services:
  - type: web
    name: mock-eligibility-server
    runtime: python
    buildCommand: ""
    startCommand: python3 mock_mcp_http_server.py
    envVars:
      - key: PORT
        value: 10000
